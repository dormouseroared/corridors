---
title: Wildlife Corridors
subtitle: "More ecological network connections!"
detail: "Wildlife needs the flow, shelter and food sources made possible by many ecological connections across our landscapes, including verges, hedgerows, woodlands, plantations, canals, meadows, floodplains, streams, rivers, forgotten pockets and we need to support, improve and extend the network."
layout: base
---
<button class="btn-large btn-success">{{ title }}</button>

<button class="btn-large btn-primary">{{ subtitle }}</button>

<button>{{ detail }}</button>

{% for post in collections.posts | reverse %}
{% if loop.index0 < 10 %}
    <h5>
    <a href="{{ post.url }}">{{ post.data.title }} &bull; {{ post.date | readableDate }}</a>
    </h5>
{% endif %}
{% endfor %}